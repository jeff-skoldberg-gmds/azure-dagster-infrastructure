load_from:
  # Each entry here corresponds to a service that exposes user code.
  - grpc_server:
      host:
        env: USER_CODE_HOST
      port:
        env: USER_CODE_PORT
      location_name: "example_user_code"
